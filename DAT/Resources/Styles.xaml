<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style TargetType="{x:Type TextBox}" x:Key="ExternalLabel">
        <Setter Property="BorderBrush" Value="Black"></Setter>
        <Setter Property="BorderThickness" Value="2px"></Setter>
        <Setter Property="Height" Value="auto"></Setter>
        <Setter Property="Width" Value="auto"></Setter>
        <Setter Property="IsReadOnly" Value="True"></Setter>
        <Style.Resources>
            <Style TargetType="{x:Type Border}">
                <Setter Property="CornerRadius" Value="3"></Setter>
            </Style>
        </Style.Resources>
    </Style>

    <Style TargetType="{x:Type TextBox}" x:Key="BaseLabel">
        <Setter Property="BorderBrush" Value="Black"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Style.Resources>
            <Style TargetType="{x:Type Border}">
                <Setter Property="CornerRadius" Value="3"/>
            </Style>
        </Style.Resources>
    </Style>

    <Style BasedOn="{StaticResource BaseLabel}" TargetType="TextBox" x:Key="Label">
        <Setter Property="FontSize" Value="60"/>
        <Setter Property="Height" Value="90"/>
        <Setter Property="Width" Value="100"/>
    </Style>

    <Style BasedOn="{StaticResource BaseLabel}" TargetType="TextBox" x:Key="Clock">
        <Setter Property="FontSize" Value="25"/>
    </Style>

    <Style TargetType="{x:Type Button}" x:Key="Button">
        <Setter Property="Height" Value="30"/>
        <Setter Property="Width" Value="65"/>

        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="1" ScaleY="1"/>
            </Setter.Value>
        </Setter>
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
        <Style.Triggers>
            <EventTrigger RoutedEvent="Button.Click">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" To="0.9" Duration="0:0:0.2" AutoReverse="True"/>
                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" To="0.9" Duration="0:0:0.2" AutoReverse="True"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Style.Triggers>

        <Style.Resources>
            <Style TargetType="{x:Type Border}">
                <Setter Property="CornerRadius" Value="3"/>
            </Style>
        </Style.Resources>
    </Style>

</ResourceDictionary>
