<Window x:Class="DAT.settings"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Inställningar" Height="205" Width="251" ResizeMode="NoResize" ShowInTaskbar="False" 
        >

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Vertical" Margin="10">
            <TextBlock Text="Matchtid:" />
            <TextBox x:Name="matchTid" Margin="15,0,0,0" HorizontalAlignment="Left" Width="30" Text="2:00"></TextBox>
        </StackPanel>
        
        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="10">
            <TextBlock Text="Färgval:" />
            <StackPanel Margin="15,0,0,0">
                <RadioButton x:Name="Blå" Content="Blå" Checked="Blå_Click"></RadioButton>
                <RadioButton x:Name="Röd" Content="Röd" Checked="Röd_Click"></RadioButton>
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin=" 0 -5 0 0">
            <Button Style="{StaticResource Button}" Content="Spara" Width="45" Height="22" Margin="2" Click="Save_Click"/>
            <Button Style="{StaticResource Button}" Content="Avbryt" Width="45" Height="22" Margin="2" Click="Cancel_Click"/>
        </StackPanel>

    </Grid>
</Window>
